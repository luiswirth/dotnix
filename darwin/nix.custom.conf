# Determinate Nix reads this file (nix-darwin's nix.enable = false, so it does
# not manage /etc/nix/nix.conf). Tracked in-repo and symlinked via
# environment.etc so daemon config stays version-controlled.
# Flakes + nix-command are already enabled by Determinate Nix.
extra-substituters = https://nix-community.cachix.org
extra-trusted-public-keys = nix-community.cachix.org-1:mB9FSh9qf2dCimDSUo8Zy7bkq5CX+/rkCWyvRCYg3Fs=
